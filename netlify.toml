[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA redirect — all routes go to index.html so React Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
