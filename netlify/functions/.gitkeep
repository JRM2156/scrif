// Netlify serverless functions will be added here as we build each service.
// Each function lives in its own file in this folder.
// They are auto-deployed when you push to GitHub and Netlify picks them up.
